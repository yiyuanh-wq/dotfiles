return {
  {
    "gbprod/nord.nvim",
    lazy = false,
    priority = 1000,
    init = function()
      -- require("nord").setup({
      --   styles = {
      --     comments = { italic = false },
      --     bufferline = {
      --       current = {},
      --       modified = { italic = false },
      --     }
      --   }
      -- })
      -- vim.cmd.colorscheme("nord")
    end,
  },
}
